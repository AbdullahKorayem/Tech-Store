import { ToChangeDirective } from './to-change.directive';

describe('ToChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new ToChangeDirective();
    expect(directive).toBeTruthy();
  });
});
